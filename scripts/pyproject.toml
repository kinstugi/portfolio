[project]
name = "portfolio-scripts"
version = "0.1.0"
description = "Build-time tooling for the terminal portfolio (image -> ASCII logo, etc.)"
requires-python = ">=3.10"
dependencies = [
    "Pillow>=10.0",
]

[tool.uv]
package = false
